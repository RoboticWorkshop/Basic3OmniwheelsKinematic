1. Create new package for ROS inside your ros workspace by using this command:
	catkin_create_pkg basic_kinematics roscpp rospy geometry_msgs std_msgs sensor_msgs

2. Copy all of the python programs into your package, and place it inside scripts directory
